apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "integrador.fullname" . }}-test-connection"
  labels:
    {{- include "integrador.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "integrador.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
